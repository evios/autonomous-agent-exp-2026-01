name: Trigger Agent Work

# Chains agent sessions after PR merge
# claude-code-action doesn't support push events, so this dispatches agent-work

on:
  workflow_dispatch:
  push:
    branches:
      - main
    paths:
      - 'agent/state/**'

jobs:
  trigger:
    runs-on: ubuntu-latest
    permissions:
      actions: write
    steps:
      - name: Dispatch agent work session
        run: gh workflow run agent-work.yml
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          GH_REPO: ${{ github.repository }}